# ============ การตั้งค่าเซิร์ฟเวอร์ ============
PORT=3000
# อายุ session (วัน) ก่อนต้องล็อกอินใหม่
SESSION_TTL_DAYS=7
# ตั้ง true เมื่อรันบน HTTPS (คุกกี้ session จะถูกส่งเฉพาะผ่าน secure connection)
COOKIE_SECURE=false

# ============ บัญชีเจ้าของระบบ (owner) เริ่มต้น ============
# ถ้ายังไม่มีผู้ใช้ role=owner ระบบจะสร้างให้อัตโนมัติตอน boot
# ⚠️ เปลี่ยนรหัสผ่านก่อนใช้งานจริง
ADMIN_EMAIL=owner@gmail.com
ADMIN_PASSWORD=123456

# ============ โหมดพัฒนา (DEV) ============
# true  = โหมดจำลอง: OTP และลิงก์ยืนยันอีเมลจะแสดงที่ console + หน้าเว็บ (ยังไม่ส่ง SMS/อีเมลจริง)
# false = โหมดจริง: ต้องมี service SMS/อีเมลจริง (ดู otp.js และ mailer.js)
DEV_MODE=true

# ============ OTP ============
OTP_TTL_MINUTES=5
OTP_MAX_ATTEMPTS=5

# ============ ฐานข้อมูล (MySQL) ============
# connection URL ของ MySQL — ระบบใช้ mysql2 ผ่านค่านี้ (หรือ MYSQL_URL ก็ได้)
# ตัวอย่าง: mysql://user:password@host:3306/dbname
DATABASE_URL=mysql://root:password@localhost:3306/shop

# ============ Google reCAPTCHA v2 ============
# ใส่ key เมื่อต้องการเปิดใช้งาน (ได้จาก https://www.google.com/recaptcha/admin)
# ถ้าว่างไว้ ระบบจะข้าม reCAPTCHA และใช้ honeypot + rate limit แทน (สำหรับ dev)
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# ============ SMS (OTP จริง) — ไม่บังคับในโหมด dev ============
# 2 ทางเลือก (เลือก provider ได้ที่หน้าแอดมิน /admin/otp.html หรือที่นี่):
# 1) Twilio — https://www.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE=
# 2) ThaiBulkSMS (API v2) — https://www.thaibulksms.com (เหมาะกับ SMS ไทย)
TBS_API_KEY=
TBS_API_SECRET=
TBS_SENDER=

# ============ SMTP (อีเมลจริง) — ไม่บังคับในโหมด dev ============
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# ============ Google OAuth (เข้าสู่ระบบด้วย Google) ============
# ว่างไว้ = โหมดทดสอบ (dev): กดปุ่ม Google จะไปหน้า mock ให้กรอกอีเมลจำลอง
# มีค่า = OAuth จริง (สร้างได้ที่ https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback


# ============ ข้อมูลทางกฎหมาย / PDPA ============
# ชื่อผู้ให้บริการ (ผู้ควบคุมข้อมูลส่วนบุคคล) และอีเมลติดต่อสำหรับใช้สิทธิ PDPA
# ค่าเหล่านี้จะแสดงในหน้าถ้อยแถลง /privacy.html
# แนะนำให้ตั้งจากหลังบ้านแทน: เมนู "ข้อมูลทางกฎหมาย (PDPA)" → /admin/legal.html (มีผลทันที ไม่ต้องรีสตาร์ท)
# ค่าใน .env นี้ใช้เป็น "ค่าถอยหลัง" เมื่อหลังบ้านยังไม่ได้ตั้งเท่านั้น
# ⚠️ LEGAL_EMAIL ต้องเป็นกล่องเมลจริงที่คุณเปิดอ่านได้ — ถ้าไม่มี ให้เว้นว่างไว้
#    (หน้าถ้อยแถลงจะซ่อนบรรทัดอีเมล แล้วบอกผู้ใช้ให้ดูช่องทางติดต่อจากหน้าเว็บแทน)
#    ห้ามใส่ที่อยู่สมมติ เช่น support@… ที่ยังไม่ได้สมัคร เพราะลูกค้าจะส่งเมลไปแล้วไม่มีใครได้รับ
LEGAL_OPERATOR=QPage — ระบบสั่งอาหารและระบบสมาชิกร้านค้า
LEGAL_EMAIL=
