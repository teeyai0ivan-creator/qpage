# dependencies
node_modules/

# env
.env

# ฐานข้อมูล (ไม่ควรอัปโหลดข้อมูลจริงขึ้น GitHub)
*.db*

# uploads (รูปที่ผู้ใช้อัปโหลด — โลโก้/เมนู ที่แสดงสาธารณะได้)
public/uploads/
# ไฟล์ส่วนตัว (สลิปโอนเงิน) — ห้ามขึ้น GitHub เด็ดขาด
private_uploads/

# ไฟล์สำรองข้อมูล (มีข้อมูลลูกค้า — ห้ามขึ้น GitHub)
backups/

# logs
*.log

# os / editor
.DS_Store
Thumbs.db

# test artifacts
.zcode/

# โปรแกรม Windows (Electron) — ติดตั้ง/ไฟล์ที่ build เองไม่ต้องเข้า git
desktop/node_modules/
desktop/dist/
desktop/.data/
